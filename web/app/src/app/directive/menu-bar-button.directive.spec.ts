import { MenuBarButtonDirective } from './menu-bar-button.directive';

describe('MenuBarButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuBarButtonDirective();
    expect(directive).toBeTruthy();
  });
});
